# Core pipeline
scikit-learn>=1.4
pyyaml>=6.0

# API
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6

# MCP server — stdio and SSE transports
mcp>=1.0
starlette>=0.37          # required by FastMCP SSE transport

# Testing
pytest>=8.0
